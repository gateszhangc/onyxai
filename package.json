{
  "name": "onyxai",
  "version": "1.0.0",
  "private": true,
  "description": "Static launch page for OnyxAI, an AI memory assistant.",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:browser": "playwright test",
    "brand:generate": "sh scripts/generate_brand_assets.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
